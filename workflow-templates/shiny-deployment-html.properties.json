{
  "name": "Deploy Plain HTML To Stats4SD Shiny Server Workflow",
  "description": "This workflow will automatically publish to the staging server when you push to the default (main) branch, and will publish to the live server when you make a new release. This workflow is for static web apps / plain html-based repos",
  "iconName": "stats4sd-logo",
  "categories": [ "R" ],
"filePatterns": [
        ".*\\.md$"
    ]

  }
  
